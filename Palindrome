# determine the reverse of our string
# compare the string to its reverse
# if it is a palindrome, print "Yes, __ is a palindrome"
# otherwise print "No, __ is not a palindrome"
name = "Laura"
 
puts name.reverse
 
def check_palindrome(word)
 if word == word.reverse
   puts "Yes, #{word} is a palindrome"
 
 else
   puts "No, #{word} is not a palindrome"
 end
